spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:8730
server:
  port: 8731

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS